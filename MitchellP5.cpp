/*
filename: MitchellP5.cpp
programmer: Nicholas Mitchell
email: nmitchell11@cnm.edu
*/

#include "Functions.h"

int main() {

	string food = "Pizza";  // Variable declaration
	string* ptr = &food;    // Pointer declaration

	cout << "food: " << food << endl;
	cout << "pointer's memory address: " << ptr << endl;
	cout << "pointers's variable: " << *ptr << endl;

	//WriteHeader();

	//do {
		//AskForNameAndShape();

		// TODO call functions that calculate how much area is in the room including ceiling, 
		// total amount of room after window calculations are found then percentage of room not being 
		// painted. Report gallons of paint needed as well.

		
	
	//} while(DoAgain());

	//GoodBye();

	return 0;
}